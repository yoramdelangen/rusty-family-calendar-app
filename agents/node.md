# Node System

- `mod.rs`: node state + draw code.
- `builder.rs`: general node builder.
- `grid_builder.rs`: grid-only builder.
- Keep state, layout flags, and draw logic here.
- Add helpers only if more than one caller needs them.
